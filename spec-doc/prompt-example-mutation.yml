gqlapi: 1.0.1
info:
  title: Sysml Server Graphql API
  description: Sysml Server Graphql API
  version: 1.0.0
mutations:
  exampleMutation:
    description: The description for exampleMutation
    tags:
      - example
      - exampleMutation
    variables:
      param_one:
        type: string
        required: true
        example: "param_one_example"
      param_two:
        type: object
        required: false
        properties:
          child_param_one:
            type: string
            required: false
            example: 'child_param_one_example'
          child_param_two:
            type: boolean
            required: false
            example: 'child_param_two_example'
    output:
      200:
        contentType: application/json
        description: something
        schema:
          type: object
          properties:
            outputData:
              type: object
              properties:
                outputDataPropOne:
                  type: string
                  example: "outputDataPropOneExample"
                outputDataPropTwo:
                  type: string
                  example: outputDataPropTwoExample
      400:
        description: Bad Request
      401:
        description: Unauthenticated
      403:
        description: Forbidden

Output: [{ 
"scenario": "successfully call to the exampleMutation with required params",
"sampleInput": { "param_one":"param_one_example" }, 
"preconditions": ["user must be authenticated", "preconditions 2", "preconditions 3"], 
"steps": ["set the authorization header with valid token", "call the exampleMutation mutation with valid input data", "observe the output"], 
"sampleOutput": {"outputData": {"outputDataPropOne": "outputDataPropOneExample", "outputDataPropTwo": "outputDataPropTwoExample"}},
"status": {"200": "something"} },
{ 
"scenario": "successfully call to the exampleMutation with additional params",
"sampleInput": { "param_one":"param_one_example", "param_two": { "child_param_one": "child_param_one_example", "child_param_two": "child_param_two_example" } }, 
"preconditions": ["user must be authenticated", "preconditions 2", "preconditions 3"], 
"steps": ["set the authorization header with valid token", "call the exampleMutation mutation with valid input data", "observe the output"], 
"sampleOutput": {"outputData": {"outputDataPropOne": "outputDataPropOneExample", "outputDataPropTwo": "outputDataPropTwoExample"}},
"status": {"200": "something"} },
{ 
"scenario": "call to the exampleMutation without required params",
"sampleInput": { "param_one":"param_one_example", "param_two": { "child_param_one": "child_param_one_example", "child_param_two": "child_param_two_example" } }, 
"preconditions": ["user must be authenticated", "preconditions 2", "preconditions 3"], 
"steps": ["set the authorization header with valid token", "call the exampleMutation mutation with valid input data", "observe the output"], 
"sampleOutput": {},
"status": {"400": "Bad Request"} },
{ 
"scenario": "call to the exampleMutation with unauthenticated user",
"sampleInput": { "param_one":"param_one_example" }, 
"preconditions": ["user must be authenticated", "preconditions 2", "preconditions 3"], 
"steps": ["do not set the authorization header with auth token", "call the exampleMutation mutation with valid input data", "observe the output"], 
"sampleOutput": {},
"status": {"401": "Unauthenticated"} },
{ 
"scenario": "call to the exampleMutation with unauthorized user",
"sampleInput": { "param_one":"param_one_example" }, 
"preconditions": ["user must be authenticated", "preconditions 2", "preconditions 3"], 
"steps": ["set the authorization header with user auth token having improper permissions", "call the exampleMutation mutation with valid input data", "observe the output"], 
"sampleOutput": {},
"status": {"403": "Forbidden"} }
]